# Vite 开发代理的 Agent Server 目标。浏览器仍通过同源 /threads、/runs 和 /api 访问。
VITE_AGENT_SERVER_TARGET=http://127.0.0.1:2024

# 通常保持为空，让 SDK 使用 window.location.origin 和 Vite proxy。
# 只有明确需要浏览器直连 Agent Server、且已正确配置 Cookie/CORS 时才填写。
VITE_LANGGRAPH_API_URL=
